# Quasar-BLAS Environment Requirements

# Note: This is a Rust-native project. Standard Python packages are not required 
# unless you are writing custom Python scripts to parse the `criterion` output.

# --- Core Toolchain ---
# rustup toolchain install nightly
# cargo

# --- Linux Profiling (Phase 6) ---
# apt-get install linux-tools-common linux-tools-generic

# --- Industry Benchmarking (Phase 6) ---
# apt-get install libopenblas-dev
# Intel oneAPI (MKL)
